/*
const db = require("../database/models");
//const sequelize = db.sequelize;
function clasifMiddleware(req, res, next) 

db.User.findByPk(req.params.id)
{
 where:{
     id_rol_fk:'2'

}

}.then(
if() {
  dato= "details";
}else{
  dato= "hidden";
}
next();
)
module.exports = clasifMiddleware;

*/
